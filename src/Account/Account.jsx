import {useTitle} from "../UseTitle.js"

import "./Account.css";

export default function Account() {
    useTitle("Account settings: Nume prenume")

    return (
        <div className="page">

        </div>
    );
}