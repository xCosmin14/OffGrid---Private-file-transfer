import {useTitle} from "../UseTitle.js"

import "./MyFiles.css";

export default function SharedFiles() {
    useTitle("Shared files")

    return (
        <div className="page">

        </div>
    );
}